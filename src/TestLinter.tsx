import React from 'react';

// Este es un comentario de prueba para el linter de commits
const TestLinter = () => {
    const unused = 'esto no se usa';

    const badFormatting = { spacing: 0 };

    return <div>Test</div>;
};

export default TestLinter;
